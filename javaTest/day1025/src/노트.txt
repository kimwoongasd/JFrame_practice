ArrayList
	* 배열대신 가장 많이 사용
	* 이미 있는 컬렉션으로 리스트를 만들 수 있다
	* 순서를 유지한다
	* 여러가지 자료형을 담을 수 있다
	

set(idx, "") : index 위치 원소를 바꾼다
add() 	: list 데이터 추가
remove(idx) : index 자리의 데이터를 삭제한다, 삭제한 값을 리턴한
remove(odj) : obj 데이터를 삭제한다